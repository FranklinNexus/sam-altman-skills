# Antigravity-Specific Notes

Use `AGENTS.md` as the shared cross-tool baseline. In Antigravity, this `GEMINI.md` can be copied into a project only if you want an Antigravity-specific override.

When the user asks for Sam Altman, read `.agents/skills/sam-altman.md`.
